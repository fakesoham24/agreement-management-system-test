# ── Core Framework ──
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
python-multipart==0.0.9
aiofiles==24.1.0

# ── Authentication & Security ──
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography>=42.0.0

# ── Configuration ──
python-dotenv==1.0.1

# ── AI / LLM ──
openai>=1.0.0
httpx>=0.25.0,<0.28.0

# ── Document Processing ──
PyPDF2==3.0.1
python-docx==1.1.2
PyMuPDF>=1.24.0

# ── OCR (Scanned PDF) ──
Pillow>=10.0.0

# ── Date Utilities ──
python-dateutil>=2.8.0

# ── Pydantic Extras ──
email-validator>=2.0.0

# ── Excel Export ──
openpyxl>=3.1.0

# ── Background Scheduler ──
APScheduler>=3.10.0

# ── PDF Invoice Generation ──
reportlab>=4.0.0

# ── Templating (used internally by FastAPI) ──
jinja2==3.1.4
